export const paths = {
    about: '/about',
    home: '/',
    map: '/map',
    platforms: {
        forecast: '/platform/:id/forecast',
        observations: '/platform/:id/observations',
        platform: '/platform/:id',
        root: '/platform/',
    }
}
