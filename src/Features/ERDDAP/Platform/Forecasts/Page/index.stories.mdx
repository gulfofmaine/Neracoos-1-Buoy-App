import { ArgsTable, Meta, Story } from "@storybook/addon-docs/blocks"

import { UnitSystem } from "Features/Units/types"
import { platform } from "stories/platform"

import { ForecastChart } from "./index"
import { data, forecast_type } from "./story-data"

<Meta title="ERDDAP/Forecast" component={ForecastChart} />

# ForecastChart

[`src/Features/ERDDAP/Platform/Forecasts/Page`](https://github.com/gulfofmaine/Neracoos-1-Buoy-App/blob/master/src/Features/ERDDAP/Platform/Forecasts/Page/index.tsx) - [Platform M01 - Wind Speed Forecast](https://mariners.neracoos.org/platform/M01/forecast/wind_speed)

A forecast chart combines current data from a platform with
forecast retrieved through the Buoy Barn API.

<Story name="Forecast Chart" args={{ unit_system: UnitSystem.english, type: forecast_type, data }}>
  {Template.bind()}
</Story>

It honors the set unit system, and if you switch
unit system while it is displayed it will smoothly transition.

<ArgsTable story="Forecast Chart" />

```tsx
<ForecastChart data={data} unit_system={UnitSystem.english} type={forecast_type} />
```

export const Template = (args) => <ForecastChart {...args} />
