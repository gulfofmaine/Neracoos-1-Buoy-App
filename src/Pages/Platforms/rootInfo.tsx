/**
 * Info that is displayed when no platforms are selected.
 */
import * as React from "react"
import { Col, Row } from "reactstrap"

/**
 * Info that is displayed when no platforms are selected.
 */
export const RootInfo: React.FunctionComponent = () => (
  <Row>
    <Col>{null}</Col>
  </Row>
)
